<?php

namespace {{ namespace }};

{{ modelImport }}
use Storyfeed\Grouping\Group;
use Storyfeed\PendingActivity;
use Storyfeed\Stories\Story;

class {{ class }} extends Story
{
    /**
     * The data one activity needs, given where it happens:
     * Storyfeed::publish(new {{ class }}(…)).
     */
    public function __construct({{ constructor }}) {}

    /**
     * The activity to publish, or null to publish nothing.
     */
    public function toFeedActivity(): ?PendingActivity
    {
        return {{ activity }};
    }

    public function headline(): string
    {
        return '{{ headline }}';
    }

    public function icon(): ?string
    {
        return null;
    }

    /**
     * One aggregate headline per axis this activity can group on.
     *
     * The axes below are the ones the recipes say can apply — not a guess.
     * Only the tokens each axis PINS are safe here; anything else can render a
     * headline that lies over a group spanning many values.
     */
    public function groups(): array
    {
        return [
{{ groups }}
        ];
    }
}
